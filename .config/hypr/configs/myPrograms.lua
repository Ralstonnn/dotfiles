-- Programs used across the config.
-- Returned as a table so other files can require() it.

return {
	terminal = "ghostty",
	fileManager = "dolphin",
	menu = "tofi-drun --drun-launch=true",
	browser = "google-chrome-stable",
}
